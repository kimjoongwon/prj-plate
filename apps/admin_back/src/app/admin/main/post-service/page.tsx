interface UserServicePageProps {
  children: React.ReactNode;
}

const PostServicePage = (props: UserServicePageProps) => {
  const { children } = props;
  return <div>PostServicePage</div>;
};

export default PostServicePage;
