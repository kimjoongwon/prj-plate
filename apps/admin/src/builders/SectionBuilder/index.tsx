// import { SectionBuilder as SectionBuilderState } from '@shared/types';

// interface SectionBuilderProps {
//   state: SectionBuilderState;
//   children: React.ReactNode;
// }

// export const SectionBuilder = (props: SectionBuilderProps) => {
//   const { state, children } = props;
// };
