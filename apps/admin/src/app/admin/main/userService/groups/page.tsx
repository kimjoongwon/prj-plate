'use client';
import { DataGrid } from '@shared/frontend';
import { observer } from 'mobx-react-lite';

const UserGroupsPage = () => {
  // const {} = useUserGroupsPage();

  return <div>user-groups</div>;
};

export default observer(UserGroupsPage);
