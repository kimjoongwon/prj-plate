'use client';

import { Button } from '@shared/frontend';

export default function Page() {
  return (
    <div>
      <Button>Button</Button>
      <Button>Button</Button>
      <Button>Button</Button>
    </div>
  );
}
