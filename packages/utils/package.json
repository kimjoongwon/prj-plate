{
  "name": "@kimjwally/utils",
  "version": "1.0.0",
  "description": "utility library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "start:dev": "tsup --config tsup.config.ts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "test": "vitest"
  },
  "author": "kimjoongwon",
  "license": "ISC",
  "devDependencies": {
    "tsconfig": "workspace:*",
    "tsup": "^7.1.0",
    "typescript": "^5.1.6",
    "vitest": "0.33.0"
  }
}
