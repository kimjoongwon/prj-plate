{
  "name": "@coc/db",
  "version": "0.0.0",
  "private": true,
  "module": "./dist/index.mjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "start:dev": "prisma generate",
    "lint": "tsc --noEmit && eslint \"src/*.ts*\"",
    "test": "vitest",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "prisma": "^5.3.1",
    "tsconfig": "*",
    "tsup": "^7.2.0"
  },
  "dependencies": {
    "@prisma/client": "5.4.1"
  }
}
