{
  "name": "core",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.16.0",
  "scripts": {
    "build": "turbo build",
    "build:server": "turbo build --filter=server",
    "build:admin": "turbo build --filter=admin",
    "build:mobile": "turbo build --filter=mobile",
    "build:storybook": "turbo build --filter=storybook",
    "clean": "turbo clean",
    "start:dev": "turbo start:dev",
    "start:mobile": "turbo start:dev --filter=mobile",
    "start:server": "turbo start:dev --filter=@shared/schema --filter=server",
    "start:admin": "turbo start:dev --filter=admin",
    "start:storybook": "turbo start:dev --filter=storybook",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "format": "turbo format",
    "test": "turbo test",
    "storybook": "pnpm -C apps/mobile storybook"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "vite": "catalog:",
    "@vitejs/plugin-react-swc": "catalog:",
    "vitest": "catalog:",
    "@vitest/ui": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
