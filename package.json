{
  "name": "core",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.16.0",
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean",
    "start:dev": "turbo start:dev",
    "mobile:dev": "turbo start:dev --filter=mobile",
    "server:dev": "turbo start:dev --filter=server",
    "admin:dev": "turbo start:dev --filter=admin",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "format": "turbo format",
    "test": "turbo test",
    "storybook": "pnpm -C apps/mobile storybook"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "vite": "catalog:",
    "@vitejs/plugin-react-swc": "catalog:",
    "vitest": "catalog:",
    "@vitest/ui": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
