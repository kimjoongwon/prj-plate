{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean",
    "dev": "turbo dev --no-cache  --continue",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo test"
  },
  "devDependencies": {
    "prettier": "^3.2.2",
    "tsup": "^8.0.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@7.15.0"
}
